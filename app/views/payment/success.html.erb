<div class="h-100-center">
    <h1 class="text-success">
    <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="currentColor" class="bi bi-check-circle-fill me-3" viewBox="0 0 16 16">
        <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0m-3.97-3.03a.75.75 0 0 0-1.08.022L7.477 9.417 5.384 7.323a.75.75 0 0 0-1.06 1.06L6.97 11.03a.75.75 0 0 0 1.079-.02l3.992-4.99a.75.75 0 0 0-.01-1.05z"/>
    </svg>
    Merci pour votre commande, <%= @customer.name %> !</h1>
    <p>Votre commande a été passée avec succès.</p>

    <h3 class="mt-5">Votre commande :</h3>
    <div class="row">
        <div class="card">
            <% @order.orderitems.each do |item| %>
                <div class="row">
                    <div class="col">
                        <img src="<%= item.item.image_url %>" alt="<%= item.item.name %>">
                    </div>
                    <div class="col">
                        <h5><%= item.item.name %></h5>
                        <p><%= item.item.price %>€ x <%= item.quantity %> = <%= item.item.price * item.quantity %>€</p>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>